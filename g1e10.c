#include <stdio.h>
#include <locale.h>
int main (){

    //definindo idioma
    setlocale(LC_ALL, "Portuguese");

   // var
    int n;

   // entradas
 for (n=1; n<=100; n++)
 {
    if (n * 2 == 0);
    printf("%i \n", n);
    n++;
 }
   //process

   // saida
return(0);
}
